# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.eggs/
build/
dist/
.pytest_cache/
.coverage
htmlcov/

# Virtual envs
.venv/
venv/
env/

# OS
.DS_Store
.fuse_hidden*
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Local artifacts (users shouldn't commit their bench output to falsify-eval itself)
*.lock.json
/tmp/
*.log

# Anything intentionally kept local — never publish via this repo
PRIVATE/

# arXiv build artefacts (regenerable from PREPRINT.md via tools/build_arxiv.sh)
arxiv/

# Mutation testing cache (regenerable; pinned to mutmut 2.x v0.2 milestone)
.mutmut-cache
mutants/

# Node / Next web app
node_modules/
apps/web/.next/
apps/web/.next.broken-*/
apps/web/.local-audits/
apps/web/dist-cli/
apps/web/tsconfig.tsbuildinfo
